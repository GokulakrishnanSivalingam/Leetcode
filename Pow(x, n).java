class Solution {
    public double myPow(double x, int n) {
        if(n>0)
        {
            return Math.pow(x,n);
        }
        else
        {
            return Math.pow(x,n);
        }
        
    }
}
